# [DEPRECATED / MERGED] Module batch-orchestrator da duoc hop nhat vao mediation-service.
# NodePort 30083 duoc tro sang billing-mediation-service (port 8080).
apiVersion: v1
kind: Service
metadata:
  name: batch-orchestrator-service
  namespace: evn-billing
spec:
  type: NodePort
  selector:
    app: billing-mediation
  ports:
  - port: 8083
    targetPort: 8080
    nodePort: 30083
    name: http
